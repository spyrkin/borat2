<Window x:Class="Chat.Gui.SimpleMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chat.Gui"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Сообщение" Height="331.107" Width="300">
    <Window.Resources>
        <ResourceDictionary>

            <Style x:Key="clearButton" TargetType="TextBox" />
        </ResourceDictionary>

    </Window.Resources>
    <Grid>
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <Label Margin="10 0 0 -10" x:Name="whois"></Label>
            </StackPanel>
            <TextBox TextWrapping="Wrap"
                     Style="{StaticResource clearButton}"
                     Background="AliceBlue"
                     HorizontalScrollBarVisibility="Disabled"
                     VerticalScrollBarVisibility="Auto" x:Name="textblock" Margin="0,16,0,0" Height="232"/>
            <Button Background="Black" Click="onSubmit" HorizontalAlignment="Right" Width="292" Margin="0,0,0,0" Content="Submit" Height="47"/>
        </StackPanel>
    </Grid>
</Window>
